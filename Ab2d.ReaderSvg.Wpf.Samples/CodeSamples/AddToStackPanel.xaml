<UserControl x:Class="Ab2d.Samples.ReaderSvgSamples.CodeSamples.AddToStackPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="White"
    >
    <Grid>
		<Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
			<Grid.RowDefinitions>
				<RowDefinition Height="30"/>
				<RowDefinition Height="104"/>
			</Grid.RowDefinitions>
			<TextBlock Grid.Row="0"  Name="DragDropMessage" HorizontalAlignment="Center" FontSize="13" FontWeight="Bold" Foreground="DarkBlue">
						 Drag and drop a svg file from windows explorer or use Load button to add a svg file
			</TextBlock>
			<Border Grid.Row="1"
					BorderThickness="2"
					BorderBrush="Black"
					Background="LightGray"
					HorizontalAlignment="Stretch"
					VerticalAlignment="Center"
					Height="104">

				<StackPanel Name="MainStackPanel" Orientation="Horizontal" Height="100">

				</StackPanel>
			</Border>
		</Grid>
		<Button Name="LoadButton" Width="100" HorizontalAlignment="Right" VerticalAlignment="Bottom" FontFamily="Verdana" BorderBrush="Black" BorderThickness="1" Click="LoadOnClick">
			Load
		</Button>
	</Grid>

</UserControl>
